# v0.4 迭代文档

> 版本: v0.4.0(重新规划)
> 更新日期: 2026-08-28
> 主题: Agent Runtime 产品化——运行可靠性、上下文治理与交互收敛
> 前置版本: [v0.3 迭代记录](./v0.3.md)
> 关联文档: [架构设计](../design/architecture.md)、[需求分析](../design/requirements-analysis.md)、[测试文档](../testing.md)、[CHANGELOG](../../CHANGELOG.md)

> 本文基于当前代码、测试和 OpenSpec 状态重新规划 v0.4，不承接历史 v0.4 任务编号，也不把已经完成的基础设施重复计入本版本。

---

## 1. 当前项目评估

### 1.1 功能成熟度

项目已经完成从“能运行 Agent”到“具备完整本地 Agent 闭环”的跃迁：

| 能力       | 当前状态        | 当前实现                                                                            |
| ---------- | --------------- | ----------------------------------------------------------------------------------- |
| Agent 编排 | ✅ 已具备       | `core/agent.py` + `core/loop.py`，自研异步 ReAct 循环                           |
| 模型层     | ✅ 已收敛       | `ai/model`、`ai/providers`、`ai/transport`、`ai/catalog`                    |
| 工具层     | ✅ 已具备       | 原子工具、MCP 适配、安全分类、执行运行时                                            |
| 会话层     | ✅ 已具备       | SessionManager、JSONL、索引、恢复、压缩、fork、`last_activity_at`                 |
| TUI        | ✅ 已具备       | 流式输出、工具块、滚动、命令、选择器、状态栏、性能优化基础                          |
| 任务模式   | ✅ 已具备       | ask / plan / code / auto 与验证、修复流程                                           |
| 测试与 CI  | ⚠️ 基础已具备 | 当前本地`948 passed`；GitHub CI 已由开发者确认通过，但质量门禁变更仍有 1 项未归档 |

### 1.2 架构成熟度

当前依赖方向已经比较清晰：

```text
CLI / TUI
    ↓
app/composition        组合根：配置、模型、工具、策略和运行时装配
    ↓
session                 会话生命周期、持久化、恢复、压缩、分叉
    ↓
core                   纯内存 Agent Runtime 与 ReAct 循环
    ↓
model / tools / policy  通过端口注入的外部能力
```

核心层已经具备以下扩展接缝：

- `transform_context`：模型请求前的上下文转换；
- `before_tool_call`：工具执行前的决策；
- `after_tool_call`：工具结果修饰或终止后续请求；
- `tool_runtime`：工具执行、超时、更新事件和资源控制；
- 结构化 Agent、turn、message、tool、usage、error、aborted 事件。

因此，v0.4 不需要重新设计主循环，也不需要将 Memory、MCP、Skill 直接塞进 `core`。更合理的方向是把这些已有接缝补齐为稳定契约。

### 1.3 当前主要问题

| 问题                                               | 影响                                   | v0.4 处理                                     |
| -------------------------------------------------- | -------------------------------------- | --------------------------------------------- |
| Runtime 的取消、异常、后续运行和资源清理路径较多   | 边界行为容易出现状态不一致             | 统一生命周期和错误收尾契约                    |
| 上下文窗口、压缩、工具结果限制分散在不同层         | 长对话可能在 Provider 侧才暴露失败     | 建立请求前预算和可解释压缩策略                |
| TUI 已有缓存和帧缓冲，但性能结果并非所有场景都改善 | 长会话体验仍缺少真实用户指标           | 以提交延迟、首 token、渲染 p95 和可输入性验收 |
| 会话已有索引和最近活动时间，但管理能力仍偏基础     | 会话增多后难以定位和整理               | 增加标题、搜索、筛选和安全归档                |
| Hook 接缝已有，但生命周期范围和异常语义不完整      | 后续记忆、审计、遥测可能重新耦合主流程 | 先建立只读观察 Hook，再开放受控上下文转换     |
| CI 已有多个 job，但质量门禁的最终阈值尚未决策      | “能跑 CI”和“可发布”之间仍有空档    | 先完成质量门禁收尾和发布检查清单              |

### 1.4 当前工程事实

- 项目版本：`0.3.0`；
- 当前本地全量测试：`uv run pytest -q`，**948 passed**；
- OpenSpec 主规格：**12/12 通过**；
- 活跃 OpenSpec 变更：`test-ci-quality-gates`，完成 **21/22**；
- 已有 TUI 性能基线和优化报告，但优化后部分场景的 frame 时间上升，不能仅凭缓存分配下降就宣称性能全面改善；
- 当前项目仍是单用户、本地、离线优先的 Agent，不具备 Web 服务、后台调度或多 Agent 运行时；
- 工作区层面的测试、架构和迭代文档仍存在历史测试数量，需要在 v0.4 阶段 0 统一校准。

---

## 2. v0.4 定位与目标

### 2.1 版本定位

v0.4 的主题不是继续扩张生态，而是：

> 让已有的单 Agent ReAct 运行时稳定到可以长期使用，并为后续记忆、审计、自动化和外部消费者提供清晰的扩展边界。

### 2.2 版本目标

1. **运行可靠**：模型异常、工具失败、确认等待、超时、取消和端口热切换都有一致行为；
2. **上下文可控**：长对话在请求前可预估、可压缩、可诊断，不依赖 Provider 返回窗口错误；
3. **交互及时**：用户输入立即回显，流式输出和工具执行不阻塞输入，状态变化不造成布局跳动；
4. **会话可管理**：用户可以快速找到、恢复、整理和删除会话；
5. **扩展低耦合**：记忆、审计和遥测通过 Hook/Transformer 注入，不污染 ReAct 主循环；
6. **发布可验证**：测试、跨平台、安装、质量检查和性能报告可重复执行。

---

## 3. 版本范围

| 领域     | v0.4 目标                                        | 优先级 |
| -------- | ------------------------------------------------ | ------ |
| 工程闭环 | 完成 v0.3 CI 质量门禁、版本校准、发布前检查      | P0     |
| Runtime  | 统一运行生命周期、异常、取消、资源清理和恢复语义 | P0     |
| 上下文   | 模型窗口预算、自动压缩、工具结果治理、诊断       | P0     |
| TUI      | 即时提交、流式响应、稳定状态栏、长会话交互       | P0     |
| Session  | 标题、搜索、筛选、归档、删除和恢复诊断           | P1     |
| Hooks    | 生命周期观察、异常隔离和受控上下文转换           | P1     |
| Tools    | 跨平台能力探测、并发/超时/输出保护               | P1     |
| Provider | 错误分类、有限重试和模型能力诊断                 | P2     |

---

## 4. 分阶段实施计划

### 阶段 0：基线和 v0.3 收尾

目标是先把当前状态变成可复核的基线，不在数据不一致的情况下开始优化。

| ID    | 工作项                        | 验收标准                                                                | 状态                                   |
| ----- | ----------------------------- | ----------------------------------------------------------------------- | -------------------------------------- |
| V4-01 | 完成`test-ci-quality-gates` | 根据至少一轮稳定 CI 数据决定覆盖率/性能阈值，任务完成并归档             | ⚠️ 21/22                             |
| V4-02 | 统一文档基线                  | README、架构、测试和迭代文档统一为最新测试数、版本和 CI 状态            | 🔲                                     |
| V4-03 | 固化发布检查                  | wheel 构建、干净环境安装、fake provider CLI、资源文件和版本一致性可复跑 | 🔲                                     |
| V4-04 | 固化性能基线                  | 记录提交回显、首 token、turn、工具、持久化、渲染和内存指标              | ⚠️ 已有 TUI 基线，需扩展用户感知指标 |

### 阶段 1：Runtime 可靠性

目标是稳定核心运行语义，而不是引入新的编排框架。

| ID    | 工作项               | 验收标准                                                                                                       | 状态 |
| ----- | -------------------- | -------------------------------------------------------------------------------------------------------------- | ---- |
| V4-05 | 运行生命周期模型     | 明确 idle、starting、model_wait、tool_running、awaiting_confirmation、continuing、completed、failed、cancelled | 🔲   |
| V4-06 | 错误分类与事件       | 模型、工具、权限、超时、取消、持久化错误使用结构化类型和稳定字段                                               | 🔲   |
| V4-07 | 取消与资源清理       | `abort`、退出、端口切换和异常路径幂等；后台任务、MCP client、subprocess 均可确认结束                         | 🔲   |
| V4-08 | steer/follow-up 边界 | steer 只在工具批次后注入，follow-up 只在当前 turn 结束后执行，不发生交错                                       | 🔲   |
| V4-09 | 失败轮次隔离         | 失败或取消不落盘不完整消息和 usage；成功后仍可继续、恢复和分叉                                                 | 🔲   |
| V4-10 | Runtime 契约测试     | 覆盖多轮 ReAct、并发工具、工具异常、超时、取消和监听器异常                                                     | 🔲   |

### 阶段 2：上下文治理

目标是让 Agent 在长对话和大工具结果下保持可预测。

| ID    | 工作项         | 验收标准                                                             | 状态                                      |
| ----- | -------------- | -------------------------------------------------------------------- | ----------------------------------------- |
| V4-11 | 统一上下文预算 | 按模型目录的窗口计算消息、工具描述、工具结果、输出预留和系统提示占用 | ⚠️ 已有模型窗口配置，需统一到请求前策略 |
| V4-12 | 请求前预算检查 | 超预算或接近阈值时在本地生成诊断，不等 Provider 返回模糊错误         | 🔲                                        |
| V4-13 | 自动压缩策略   | 按可配置阈值触发；保留系统约束、最近轮次、工具错误和必要文件变更     | ⚠️ 已有手动/阈值压缩，需补齐策略和诊断  |
| V4-14 | 工具结果治理   | 大结果提供预览、截断或摘要，保留路径、退出码、错误和变更信息         | 🔲                                        |
| V4-15 | 上下文恢复回归 | 压缩、恢复、分叉、多次压缩和模型切换后语义及父级链保持正确           | 🔲                                        |
| V4-16 | 上下文可见诊断 | TUI/CLI 能说明当前占用、触发压缩的原因和被裁剪结果范围               | 🔲                                        |

### 阶段 3：TUI 交互收敛

目标是围绕真实用户感知优化，而不是单纯增加缓存层。

| ID    | 工作项             | 验收标准                                                           | 状态                                |
| ----- | ------------------ | ------------------------------------------------------------------ | ----------------------------------- |
| V4-17 | 提交即时回显       | Enter 后先显示用户消息和运行状态，再初始化模型和工具请求           | 🔲                                  |
| V4-18 | 流式响应不阻塞输入 | 高频增量、工具执行和长 Markdown 回复下仍能输入、取消和滚动         | ⚠️ 已有事件帧缓冲，需端到端验证   |
| V4-19 | 状态栏稳定布局     | 动态阶段、耗时、任务验证和上下文变化只更新固定槽位，不移动静态内容 | ✅ 已有基础，需纳入回归基线         |
| V4-20 | 工具状态完整展示   | 排队、执行、确认、成功、失败、超时、取消、结果截断状态均可区分     | ⚠️ 已有主要状态，需补齐边界状态   |
| V4-21 | 长会话渲染         | 历史内容按需物化；展开工具结果、resize、滚动和点击映射不阻塞输入   | ⚠️ 已有缓存优化，需以用户指标验收 |
| V4-22 | TUI 性能回归       | 固定 fixture 测量提交延迟、首 token、渲染 p50/p95、内存和帧丢失    | 🔲                                  |

### 阶段 4：Session 可管理性

目标是在已有 JSONL、索引、`last_activity_at`、fork 和 tree 基础上改善日常使用。

| ID    | 工作项       | 验收标准                                                       | 状态                              |
| ----- | ------------ | -------------------------------------------------------------- | --------------------------------- |
| V4-23 | 会话标题     | 支持安全的自动标题或手动重命名；标题不破坏历史消息和父级链     | 🔲                                |
| V4-24 | 搜索和筛选   | 按标题、时间、模型、状态筛选；索引失效时可回源重建             | 🔲                                |
| V4-25 | 归档和删除   | 归档不删除 JSONL；删除和批量操作有确认、失败诊断和权限保护     | 🔲                                |
| V4-26 | 恢复诊断     | 损坏、过期或不兼容会话可局部降级，并说明可操作的恢复方式       | 🔲                                |
| V4-27 | 会话列表性能 | 会话数量增加时列表和最近会话使用索引，不对所有文件重复全量解析 | ⚠️ 已有索引，需补场景和失效测试 |

### 阶段 5：Hooks 与上下文扩展

目标是为后续记忆、审计和遥测提供扩展接缝，不实现完整插件运行时。

| ID    | 工作项                  | 验收标准                                                                             | 状态                      |
| ----- | ----------------------- | ------------------------------------------------------------------------------------ | ------------------------- |
| V4-28 | 生命周期观察 Hook       | 支持 turn、model、tool、session 的开始、更新和结束观察；Hook 不修改主流程            | 🔲                        |
| V4-29 | Hook 异常隔离           | Hook 异常有诊断，不破坏 Agent 运行、持久化和取消收尾                                 | 🔲                        |
| V4-30 | ContextTransformer 契约 | 明确输入、输出、作用域、顺序、超时和失败回退语义                                     | ⚠️ 已有端口，需补齐契约 |
| V4-31 | Hook 装配边界           | 具体实现只由`app/composition` 装配，`core` 不导入 memory、MCP、Skill 或 provider | 🔲                        |
| V4-32 | 扩展契约测试            | 覆盖多个 Hook 顺序、取消、异常、上下文修改和工具结果修改                             | 🔲                        |

### 阶段 6：工具和 Provider 稳定性

该阶段只有在 P0/P1 完成且有实际问题证据时推进。

| ID    | 工作项              | 验收标准                                                              | 状态 |
| ----- | ------------------- | --------------------------------------------------------------------- | ---- |
| V4-33 | 工具能力探测        | 识别 shell、平台、外部检索器和权限能力；缺失依赖有明确诊断            | 🔲   |
| V4-34 | 工具资源保护        | 并发上限、超时、输出截断、内存限制和取消后的子进程清理可配置          | 🔲   |
| V4-35 | 外部检索器 fallback | `rg`/`fd` 只作为可选加速；不可用、失败或平台不支持时回退纯 Python | 🔲   |
| V4-36 | Provider 错误分类   | 区分网络、限流、认证、参数不支持和服务端错误                          | 🔲   |
| V4-37 | 安全重试            | 只对安全的模型请求有限重试；不重复执行有副作用的工具                  | 🔲   |
| V4-38 | 模型能力诊断        | `/model` 或 `/status` 展示上下文窗口、思考、工具调用和缓存能力    | 🔲   |

---

## 5. 架构约束

### 5.1 ReAct 主循环不承载业务扩展

主循环只负责：

```text
读取上下文
  → 请求模型
  → 解析 assistant 内容和 tool calls
  → 执行工具
  → 写入结果
  → 判断是否继续
```

以下能力不得通过修改循环条件直接接入：

- 长期记忆；
- MCP 发现和连接管理；
- Skill 文件加载；
- 会话 JSONL 读写；
- TUI 状态和渲染；
- Provider 选择和配置读取。

### 5.2 扩展接入方式

```text
app/composition
    ├── ModelPort
    ├── Tool / ToolRuntime
    ├── ApprovalPolicy
    ├── ContextTransformer
    └── RuntimeObserver / Hook
            ↓
core.Agent Runtime
            ↓
structured events
            ↓
session / TUI / CLI / audit / telemetry
```

Hook 首版只允许观察和受控转换：

- 观察 Hook 默认只读；
- 上下文修改必须经过显式 Transformer；
- Transformer 必须受会话、项目和大小范围限制；
- 扩展失败时遵守明确的回退、取消和回滚语义；
- 不在 v0.4 执行任意第三方插件代码。

### 5.3 兼容性原则

v0.4 的优化不能破坏：

- JSONL append-only 格式；
- 会话恢复、压缩、fork 和父级链；
- `last_activity_at` 排序语义；
- 工具调用 ID 与结果归属；
- 权限确认和拒绝策略；
- `abort / steer / followup` 语义；
- AI 层与 core 的导入边界；
- Windows、Linux、macOS 的可用性。

---

## 6. 非目标

以下功能不作为 v0.4 承诺：

| 功能               | 延后原因                                             |
| ------------------ | ---------------------------------------------------- |
| 自动长期记忆       | 先通过 ContextTransformer 验证作用域、隐私和删除语义 |
| 完整插件系统       | 需要独立的信任、沙箱、权限和依赖协议                 |
| 多智能体协作       | 会扩大调度、会话、工具归属和交互复杂度               |
| Web/HTTP 服务      | 当前缺少真实消费者和部署边界                         |
| Automations        | 需要后台任务、调度、通知和持久化状态                 |
| MCP 完整协议生态   | v0.3 最小协议面先保持稳定                            |
| 数据库替换 JSONL   | 没有基准证据证明必须更换权威存储                     |
| 自动模型路由       | 容易把 Provider 策略与 Runtime 耦合                  |
| 跨平台绝对性能 SLO | 先用同一 fixture 做相对回归和趋势判断                |

---

## 7. 完成定义(DoD)

v0.4 完成时必须满足：

1. Agent 在正常、工具异常、模型异常、确认等待、取消、超时和端口切换路径下都有稳定的结构化事件和收尾语义；
2. 失败或取消轮次不会落盘不完整消息、usage 或会话元数据；
3. 上下文预算、自动压缩和工具结果治理在模型调用前可预测，并且有用户可见诊断；
4. 用户提交后立即获得回显；TUI 在长回复、长会话和高频事件下仍可输入、滚动和取消；
5. 状态栏固定槽位、工具状态和错误状态的视觉契约有回归测试；
6. 会话支持标题、搜索、筛选、归档/删除确认和恢复诊断；
7. Hook 和 ContextTransformer 只通过协议和组合根接入，异常不会破坏主循环；
8. 工具在 Windows、Linux、macOS 上具备能力探测、超时和 fallback；
9. Provider 重试不会重复执行有副作用的工具调用；
10. `uv run pytest -q`、分层测试、OpenSpec 校验、`git diff --check`、构建安装冒烟和 GitHub CI 全部通过；
11. README、架构设计、测试文档、CHANGELOG 和本迭代文档中的状态、版本和测试基线一致。

### 7.1 推荐指标

指标用于同一环境、同一 fixture 下的相对比较，不直接作为跨机器绝对承诺：

| 指标            | 关注点                           |
| --------------- | -------------------------------- |
| submit-to-echo  | 用户提交后消息出现在 TUI 的延迟  |
| submit-to-model | 用户提交到模型请求开始的本地延迟 |
| TTFT            | 请求发出到首个模型增量的时间     |
| turn latency    | 一轮 Agent 从开始到完成的总耗时  |
| tool latency    | 工具调用从开始到结果完成的耗时   |
| render p50/p95  | TUI 帧渲染耗时和长回复下的尾延迟 |
| restore latency | 会话列表和恢复耗时               |
| peak memory     | 长会话和长输出的峰值内存         |

---

## 8. 风险与控制

| 风险                     | 控制措施                                                  |
| ------------------------ | --------------------------------------------------------- |
| 运行生命周期抽象过重     | 先冻结事件和错误契约，使用普通 Python 状态和行为测试      |
| 自动压缩丢失关键信息     | 保留系统约束、最近轮次、工具错误和文件变更，并提供诊断    |
| TUI 性能优化改变视觉语义 | 纯组件测试、Textual 适配测试和窄终端人工检查并行          |
| Hook 造成新的反向依赖    | core 只依赖协议，组合根负责装配，增加导入边界测试         |
| 重试重复副作用工具       | 默认不重试工具；需要幂等证明和明确策略才允许重试          |
| 外部工具平台差异         | 能力探测、参数限制、纯 Python fallback 和平台测试同时保留 |
| CI 性能门禁误报          | 使用确定性 fixture、相对基线和趋势报告，稳定后再阻断      |
| 版本范围膨胀             | 每个新生态能力单独提出 OpenSpec 变更，不隐式加入 v0.4     |

---

## 9. 后续版本接缝

```text
v0.4 Runtime 产品化
        │
        ├── ContextTransformer
        │       └── v0.5 显式项目记忆
        │
        ├── RuntimeObserver / Hook
        │       ├── 审计与遥测
        │       └── 自动化任务事件消费
        │
        ├── 稳定 Tool / MCP 契约
        │       └── 后续插件分发和工具生态
        │
        └── 稳定 Session / Event API
                ├── Web / HTTP 消费者
                └── 多 Agent 协作
```

上述能力只有在 v0.4 的运行时、权限、会话和扩展边界稳定后重新评估，不构成 v0.4 的隐式承诺。

---

## 10. 当前状态与启动顺序

截至 2026-08-28：

- v0.4 已重新规划，功能任务尚未开始；
- v0.3 当前本地全量测试为 **948 passed**；
- `openspec validate --specs` 为 **12/12**；
- `test-ci-quality-gates` 为 **21/22**，应先完成 V4-01；
- TUI 性能基线、缓存和帧缓冲已有实现，但需要扩展到用户感知指标；
- 上下文窗口、压缩、Hook 端口和会话索引已有基础，应优先做契约统一和缺口测试。

推荐启动顺序：

```text
V4-01~V4-04 工程基线
        ↓
V4-05~V4-10 Runtime 可靠性
        ↓
V4-11~V4-16 上下文治理
        ↓
V4-17~V4-22 TUI 交互收敛
        ↓
V4-23~V4-27 Session 可管理性
        ↓
V4-28~V4-32 Hooks 扩展接缝
        ↓
V4-33~V4-38 按证据推进工具和 Provider 稳定性
```
