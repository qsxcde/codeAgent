# v0.3 迭代文档

> 版本: v0.3(任务定义)
> 更新日期: 2026-08-19
> 关联文档: [需求分析](../design/requirements-analysis.md)(FR / NFR / F-xx 编号出处)、[架构设计](../design/architecture.md)、[v0.1 迭代记录](./v0.1.md)、[v0.2 迭代记录](./v0.2.md)

---

## 1. 迭代概述

### 1.1 迭代目标

在 v0.2「好用」(会话可恢复/可切换/可压缩/可分叉 + 安全 + TUI 完整)之上推进**生态成型**(F-18~F-24,§12.3 v0.3 验收):

1. **扩展生态**:Skills 按需加载(F-18)、MCP 外部工具接入(F-20)——对标 Claude/Codex Skills;插件系统(F-19)经评估移出本迭代(见 E5,能力已被技能 + MCP 覆盖);
2. **体验差异**:成本透明(F-22)——信任赤字对冲(需求分析 §10.2:成本透明是社区高赞、与事件流天然契合的差异化);轻量记忆(F-21)经评估移出本迭代(见 E4);
3. **平台与导航**:会话树 UI(F-23 剩余,分支 fork 已在 v0.2 提前落地)、Web/HTTP 事件流订阅(F-27,承接 v0.2 对 F-24 平台部署的改写决策)。

对标 Pi(earendil-works/pi)的 resource-loader / skills 体系(2026-08-15 源码实查 AGENTS.md 与 compaction,2026-08-19 实查 skills.ts);**注意:Pi 无插件系统**(仓库不存在 extensions/loader.ts,此前"对齐 Pi extensions"引用失效,见 E5)。

### 1.2 迭代范围

| 模块 | 范围 |
|---|---|
| `resources/skills/` | ✅ 已落地:SKILL.md 技能格式 + 多源按需加载 + 渐进式披露(名称/描述进 system prompt,**正文经 skill 工具可见**)+ TUI `/skills` 手动加载(E1~E3) |
| MCP | 客户端适配(协议最小面:tools/list → 工具接入)+ 工具数分组预算 |
| 成本 | usage 落库(DR-6)+ 费用估算(单价表)+ 展示(状态栏/事件流/CLI) |
| 会话树 | fork 链数据视图 + TUI `/tree` 导航(`/sessions` 父子展示增强) |
| Web | HTTP 服务 + 事件流订阅(SSE)+ 会话接入(create/run/subscribe) |
| `ai/` | `ModelSpec` 增价格元数据(仿 Pi Model);usage 归一与估算 |
| `tests/` | 全新增量离线测试(mock MCP server / 临时目录树 / FakeClient / 桩服务) |

### 1.3 非目标(明确移出本迭代)

| 项 | 去向 |
|---|---|
| 多智能体协作(F-25) | 远期(事件流天然适配,原型另行规划) |
| Automations 定时任务(F-26) | 远期 |
| 平台部署 `langgraph.json`(F-24) | **随 v0.2 编排自研永久调整**:已改写为 HTTP/事件订阅入口(并入本迭代 F-27) |
| 完整 MCP 生态(采样/溯源/权限模型) | 迭代内做最小协议面;扩展留后续 |
| 轻量记忆(F-21) | **2026-08-19 定案移出**(E4):价值域已被会话持久化 + AGENTS.md 覆盖;后续按需重估 |
| 插件系统(F-19) | **2026-08-19 定案移出**(E5):能力已被技能(分发)+ MCP(工具)覆盖;推迟到 MCP 落地后重估 |

### 1.4 完成定义(DoD)

1. **Skills**:✅ 已落地(2026-08-19,E1~E3)——`resources/skills/` 目录与 SKILL.md 格式定义;按需加载 + 渐进式披露(描述入 system prompt,**正文经 `skill` 工具按需获取**,原"经 read 可见"机制变更,见 E2);加载结果可见可断言(`/skills` 列表 + `/status` 技能区与遮蔽诊断);
2. **MCP**:客户端接入外部工具(工具 schema 转换);工具数分组预算策略生效(控制提示词膨胀);
3. **成本透明**:usage 落库(DR-6);费用估算(单价表 + 输出/输入/推理 token);可见位置(状态栏/事件流/CLI)至少一处落地;
4. **会话树 UI**:fork 链可导航(`/tree` 或 `/sessions` 父子展示);切换沿用订阅跟随;
5. **Web/HTTP**:事件流订阅(SSE)可消费;会话 create/run/subscribe 至少一闭环;
6. **全量测试离线全绿**:`uv run pytest` 全量通过(阶段 1 完成后基线 590/590),零网络、零密钥(MCP/HTTP 均注入 mock/桩)。

### 1.5 时间线

| 日期 | 事件 |
|---|---|
| 08-15 | 建稿:v0.3 任务定义(基于 v0.2 完成状态 + 需求分析 §10.1 路线图 + §12.3 验收) |
| 08-19 | 阶段 1 Skills 完成(T-49~T-52 + 技能工具/`/skills` 扩展,归档 `2026-08-19-skills-system`,590/590 全绿);F-21 轻量记忆定案移出(E4) |
| 08-19 | F-19 插件系统定案移出(E5);阶段重编号:阶段 2 MCP / 阶段 3 成本透明 / 阶段 4 会话树 UI / 阶段 5 Web / 阶段 6 收尾 |
| 待排 | 阶段 2 MCP |
| 待排 | 阶段 3 成本透明 |
| 待排 | 阶段 4 会话树 UI |
| 待排 | 阶段 5 Web/HTTP |
| 待排 | 全量验收 |

---

## 2. 任务分解

**图例**:
- 优先级:**P0** = 闭环必须 / **P1** = 本迭代内尽量;
- 需求出处:F-xx = 需求分析修订功能清单;§12.3 = v0.3 验收;
- 状态:🔲 未开始 / ⚠️ 部分完成 / ✅ 已完成;
- 对齐说明:对照 Pi 的实现点(2026-08-15 源码实查)。

### 阶段 1:Skills 技能系统(F-18,对齐 Pi skills.ts)

| ID | 任务 | 说明 / 验收要点 | 优先级 | 出处 | 状态 |
|---|---|---|---|---|---|
| T-49 | SKILL.md 格式与目录 | `resources/skills/` 目录约定(一技能一目录,`SKILL.md` 头:name/description/frontmatter);格式校验纯函数(缺 name/description 报诊断) | P0 | F-18 | ✅ |
| T-50 | 加载器与注册表 | `load_skills(cwd, config_dir)` 纯函数:发现 / 去重 / 诊断(对齐 Pi `skills.ts` 的 `loadSkills`);注册表排序(name) | P0 | F-18 | ✅ |
| T-51 | 渐进式披露与注入 | 技能描述(一行)注入 system prompt(与 AGENTS.md 同管线,组合根 `_build_system_prompt` 扩展);正文不预载——模型按需经 **`skill` 工具**获取(机制变更,原"read SKILL.md",见 E2) | P0 | F-18 | ✅ |
| T-52 | 测试与可见性 | 临时目录树测试(发现/去重/诊断/注入顺序);`/status` 或 `/skills` 显示可用技能(加载结果可见) | P1 | F-18 | ✅ |
| T-52b | 技能工具 + `/skills` 手动加载 | **扩展任务**(探索期新增):`skill` 原子工具(name 寻址,组合根预渲染注册表注入);TUI `/skills` 命令——无参列表 / 带参手动加载(渲染块注入会话并立即触发一轮回复)/ 输入框技能名补全候选 | P1 | F-18 | ✅ |

### 阶段 2:MCP 客户端(F-20)

| ID | 任务 | 说明 / 验收要点 | 优先级 | 出处 | 状态 |
|---|---|---|---|---|---|
| T-53 | MCP 客户端最小面 | `tools/list` / `tools/call`(stdio 或 HTTP 其一,MVP 建议 stdio);协议客户端纯封装(经注入通道,mock server 离线测) | P0 | F-20 | 🔲 |
| T-54 | 工具接入与分组预算 | MCP 工具 schema → `AtomicTool` 形状转换;工具数分组预算策略(总工具数上限 + 每服务分组,防提示词膨胀) | P0 | F-20 | 🔲 |
| T-55 | 测试 | mock MCP server(内存脚本化 `tools/list`/`tools/call`);分组预算断言(超限裁组/警告) | P1 | F-20 | 🔲 |

### 阶段 3:成本透明(F-22,DR-6)

| ID | 任务 | 说明 / 验收要点 | 优先级 | 出处 | 状态 |
|---|---|---|---|---|---|
| T-56 | usage 落库 | usage 事件(已存在)→ 会话级聚合落库(JSONL `usage` entry 或 `meta` 聚合;读侧会话统计);`SessionStore` 两后端一致 | P0 | F-22、DR-6 | 🔲 |
| T-57 | 费用估算 | `ModelSpec` 增价格元数据(输入/输出/推理每百万 token 单价,目录补充);`estimate_cost(usage) -> (input_cost, output_cost, total)` 纯函数 | P0 | F-22 | 🔲 |
| T-58 | 展示 | 可见位置:CLI `--prompt` 尾部用量/费用行、TUI `/status` 或状态栏、事件流(usage 事件已含原始值)——至少一处;headless 默认展示 | P1 | F-22 | 🔲 |
| T-59 | 测试 | 估算纯函数(单价表 + 组合 token);落库两后端;展示断言 | P1 | F-22 | 🔲 |

### 阶段 4:会话树 UI(F-23 剩余,分支 fork 已 v0.2 落地)

| ID | 任务 | 说明 / 验收要点 | 优先级 | 出处 | 状态 |
|---|---|---|---|---|---|
| T-60 | 会话树数据视图 | fork 链遍历(`parentSession` + `parent_id` 链);`SessionManager.tree(session_id)` 或 store 级查询,返回树结构(纯函数可测) | P0 | F-23 | 🔲 |
| T-61 | 树导航 | TUI `/tree` 命令(展示 fork 链 + 切换节点,订阅跟随既有);`/sessions` 父子关系展示增强 | P1 | F-23 | 🔲 |

### 阶段 5:Web/HTTP API(F-27,承接 F-24 改写)

| ID | 任务 | 说明 / 验收要点 | 优先级 | 出处 | 状态 |
|---|---|---|---|---|---|
| T-62 | HTTP 服务 | 轻量 HTTP 服务(标准库或既有依赖;不新增重型框架):会话 create/run/subscribe 闭环;事件流 SSE 订阅暴露 | P0 | F-27、§1.3 | 🔲 |
| T-63 | 测试 | 桩传输/注入 server(离线);SSE 事件流断言;订阅跟随语义 | P1 | F-27 | 🔲 |

### 阶段 6:测试与收尾

| ID | 任务 | 说明 / 验收要点 | 优先级 | 出处 | 状态 |
|---|---|---|---|---|---|
| T-64 | 全量离线测试与验收 | 各阶段增量全量离线测试(MockTransport/桩/Mock MCP/内存后端);`uv run pytest` 全绿(基线 590/590);按 §6 验收核对表逐项确认 | P0 | NFR-M2 | 🔲 |

---

## 3. 任务状态总览

| 统计项 | 数量 |
|---|---|
| 任务总数 | 17(T-49~T-64 + T-52b;F-21 记忆 / F-19 插件移出,见 E4/E5) |
| 未开始 | 12 / 17 |
| 部分完成 | 0 / 17 |
| 已完成 | 5 / 17(阶段 1 全部) |

---

## 4. 演进与变更记录

| # | 变更 | 影响任务 | 说明 |
|---|---|---|---|
| E1 | 阶段 1 Skills 落地(格式/加载器/注入) | T-49/T-50 | OpenSpec change `skills-system`(归档 `2026-08-19-skills-system`):探索期定案 D1~D4——三源发现(内建 `resources/skills/` / 个人级 `<config_dir>/skills/` / 项目级 `<cwd>/.codeagent/skills/`,布局 `.codeagent/skills/<name>/SKILL.md` 对齐 Claude);同名遮蔽 **个人 > 项目 > 内建**(Claude 官方语义,与信任方向同向,恶意仓库技能压不过个人技能);新增依赖 **pyyaml**(生态 frontmatter 含块标量/引号,手写子集解析会静默误读);name 缺省目录名 / description 缺省正文第一段(生态 SKILL.md 复制即用);`app/skills.py` 纯函数镜像 `app/agents.py`;内置示例技能 2 个(commit-message / dependency-audit) |
| E2 | 技能正文获取机制变更(read → skill 工具) | T-51 | 探索期对照行业(Claude/Codex/Pi 均为客户端/harness 注入,不走模型文件工具)定案:新增 `skill` 原子工具(name 寻址,返回渲染块 `<skill name=... location=...>` 对齐 Pi `formatSkillInvocation`),替代"模型 read SKILL.md"——免工作区越界警告噪声、安全分类器零改动、name 寻址零路径拼错;注册表由组合根预渲染注入(`make_tools(cfg, skills=...)`),工具层不跨层;DoD「正文经 read 可见」措辞同步修订 |
| E3 | TUI `/skills` 手动加载扩展 | T-52b | 用户想要的技能"提示词表达不出"时的确定性出口(Claude user-invocable 语义):`/skills` 无参列表(同 `/tools` 先例)、`/skills <name>` 渲染块以 `[用户手动加载技能: <name>]` 注入会话并立即触发一轮回复(`session.run(block)`,不依赖模型自主调工具)、输入框 `/skills ␣` 技能名模糊补全候选;`/status` 增技能列表 + 遮蔽诊断(加载结果可见);两条路径(模型工具 / 用户注入)共用同一渲染函数;变更归档时同步主 specs(core / tools / tui / **skills 新能力**);全量 `uv run pytest` 590/590 全绿;历史遗留变更 `tui-login-command` / `fix-tui-visual-test-issues` 同日一并归档并同步主 specs(tui) |
| E4 | 轻量记忆(F-21)移出 v0.3 | T-60~T-62(原编号) | 探索期评估定案(2026-08-19,对齐 Pi/Codex 源码实查):**跨对话记忆对 code agent 价值域窄**——会话内上下文已被 v0.2 会话持久化(恢复/继续/分叉)覆盖;项目事实属 AGENTS.md/代码领地,用户级自动记忆跨项目注入是噪声(项目 scope 又超 MVP);用户级稳定偏好极少且可一次写进 `~/.codeagent/AGENTS.md`(Pi 无记忆系统生态成熟可用,Codex 官方文档亦言"重要内容放 AGENTS.md");F-21 移出,任务重编号(T-63~T-71 → T-60~T-68),后续按需重估 |
| E5 | 插件系统(F-19)移出 v0.3 | T-53~T-56(原编号) | 探索期评估定案(2026-08-19):**"对齐 Pi extensions/loader.ts" 引用失效**——实查 earendil-works/pi 仓库不存在该文件,Pi 无插件系统(`.pi/extensions` 仅开发脚本,agent-harness 无 extension 概念);对照主流(Claude 声明式 + 外部进程、Qwen Code 2026-08-13 原生支持 Agent Plugins v1 仅执行 skills+MCP、Codex/Pi/Gemini 无插件)与 D1 推导(插件工具必须走 MCP)→ **插件 = 生态的最后一块砖**(分发机制,前提是生态),能力已被技能(分发)+ MCP(工具)覆盖;F-19 移出,阶段重编号(阶段 3 MCP → 阶段 2,任务 T-57~T-71 → T-53~T-64),推迟到 MCP 落地后重估 |

---

## 5. 当前遗留与后续安排

### 5.1 v0.2 移入项(本迭代落地)

- **F-23 会话树 UI**:分支 fork 已随 v0.2 T-42 提前落地(`session-fork`),本迭代只做剩余导航部分;
- **F-24 平台部署改写**:v0.2 §1.3 定案——`langgraph.json` 平台部署永久调整,改写为 HTTP/事件订阅入口,并入本迭代 F-27。

### 5.2 本迭代已知风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| MCP 协议面过大 | 迭代周期拉长 | 最小面(tools/list、tools/call)+ 分组预算;扩展留后续 |
| 成本估算与真实计费偏差 | 信任赤字 | 标注"估算";单价表可配置(models.json 覆盖) |
| 提示词膨胀(工具/技能注入) | 上下文成本上升 | ✅ Skills 侧已闭环:渐进式披露(仅描述入 prompt,正文经 skill 工具按需获取);MCP 分组预算(T-54)同思路 |
| 技能同名冲突(项目技能压过个人技能) | 用户技能被仓库覆盖 | ✅ 已闭环:同名遮蔽 个人 > 项目 > 内建(Claude 语义,信任同向);遮蔽关系经诊断可见(`/status` 技能诊断) |

### 5.3 后续安排(远期)

多智能体协作(F-25)、Automations 定时任务(F-26)、Web 深化与多端(F-27 剩余)、Windows 全平台验证(F-28 已闭环,保持)、轻量记忆(F-21,E4 移出)、插件系统(F-19,E5 移出,推迟到 MCP 落地后按需重估)。

---

## 6. 验收核对表

| 完成定义(§1.4) | 验收要点 | 状态 |
|---|---|---|
| 1. Skills 按需加载 | SKILL.md 格式 + 发现/去重/诊断;描述入 system prompt,**正文经 skill 工具渐进披露**;`/skills` 列表与 `/status` 技能区可见(2026-08-19 落地,E1~E3) | ✅ |
| 2. MCP 接入 | 外部工具经客户端接入;分组预算生效 | 🔲 |
| 3. 成本透明 | usage 落库 + 费用估算 + 至少一处可见 | 🔲 |
| 4. 会话树 UI | fork 链可导航;切换沿用订阅跟随 | 🔲 |
| 5. Web/HTTP | SSE 事件流订阅;会话 create/run/subscribe 闭环 | 🔲 |
| 6. 全量离线全绿 | `uv run pytest` 全量通过(阶段 1 完成后基线 **590/590**,零网络零密钥) | ✅ 2026-08-21 审计 12 条缺陷已全部修复闭环(测试门禁 588→598 项,见 §6.5) |

### 6.5 2026-08-21 审计修复补记(闭环)

综合审计([audit-2026-08-21.md](../review/audit-2026-08-21.md),22 并行 subagent,12 条 CONFIRMED)全部缺陷已于当日修复,`uv run pytest` 598 项全绿:

| 缺陷 | 修复 |
| --- | --- |
| S-1/S-2/M-5/M-6 确认环五类绕过(命令替换/尾接白名单/紧贴分隔符/嵌套 shell/前缀匹配) | `security.py` allowlist 短路移除、规则逐逻辑段判定并集;`_dangerous_intent` punctuation 分词 + 嵌套 shell/命令替换递归检测;黑名单正则锚定扩为分隔符/引号字符类;补 find -delete/-exec、git clean 组合旗标、dd 写设备、嵌套 shell、解释器 -c 等 ask 规则 |
| S-3 API key 零确认外传 | `.env`/`~/.codeagent` 配置目录 token 级硬拒绝(bash 与 read/write/edit 双入口);`_bash_env` 收敛为白名单变量 |
| S-4 门禁不可复现(M-1/M-2/M-9) | 两条失败测试改平台无关/离线安全;`or True` 死断言改精确断言;压缩测试补真截断断言 |
| M-3 recursion 边界先执行工具后报错 | 最后一轮请求工具在 emit/执行前直接 raise |
| M-4 损坏会话文件阻断枚举 | JSON 残缺行容错跳过;`list()` 逐文件隔离损坏会话 |
| M-7 TUI 首启无 key 崩溃 | 端口与摘要器延迟装配(`_LazyPorts`/`_LazySummarizer`),/login 首启流可达 |
| M-10 会话文件世界可读 | JSONL 0600、sessions 目录 0700 |

---

## 7. 参考

- 需求基线:[requirements-analysis.md](../design/requirements-analysis.md)(F-18~F-24 定义、§10.1 路线图、§12.3 v0.3 验收、DR-6)
- 架构设计:[architecture.md](../design/architecture.md)(resources/extensions 延后档、端口-适配器模式)
- 上一迭代:[v0.2.md](./v0.2.md)(F-23 fork 提前落地、F-24 改写决策、§5.3 后续安排)
- 对齐实现:Pi `skills.ts`(✅ 已对照落地,E1~E3;2026-08-15 源码实查);**Pi 无插件/记忆系统**(2026-08-19 实查,`extensions/loader.ts` 不存在,见 E5);Claude Code Skills 官方文档(2026-08-19 实查:目录布局、frontmatter 缺省语义、同名优先级 个人>项目>内建);Qwen Code Agent Plugins v1(2026-08-19 实查:仅执行 skills+MCP,E5)
