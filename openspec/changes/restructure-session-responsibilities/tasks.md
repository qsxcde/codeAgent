## 1. 建立协议与异步持久化边界

- [x] 1.1 盘点 `session` 的生产代码、测试和文档导入关系，列出需要保留的 `codeagent.session` 包级公开导出与需要移除的兼容模块。
- [x] 1.2 在 session 领域定义持久化存储、摘要器、策略、上下文变换器和关闭器的最小 `Protocol`，将 `AgentSession`、`SessionManager` 和压缩服务的公共 `Any` 参数替换为明确类型。
- [x] 1.3 实现统一的异步持久化边界：以整次原子操作为单位在线程中调用同步 JSONL 后端，并跟踪取消后的后台操作直到结果确定。
- [x] 1.4 将轮次提交、压缩记录追加及其它异步运行路径中的同步持久化调用接入该边界，保留现有锁、回滚、索引和错误语义。

## 2. 拆分 session 运行职责

- [x] 2.1 从 `AgentSession` 提取运行轮次协调、历史提交和收尾逻辑，使门面只负责依赖组装、状态暴露和流程委托。
- [x] 2.2 将运行控制器中的确认队列、事件映射、状态推进和最终化协调拆为内聚模块，保持取消、确认超时和 cleanup 状态语义。
- [x] 2.3 将 `SessionManager` 的活动会话注册、当前会话切换、释放和关闭等待拆分，确保同一时刻仍只有一个会话运行。
- [x] 2.4 让拆分后的模块维持既有父子会话、恢复最近会话、标题、用量和生命周期行为，并确保生产文件不超过 300 行、核心函数不超过 80 行。

## 3. 拆分持久化职责

- [x] 3.1 将 JSONL 存储中的文件访问、批量提交、索引读写、分叉文件构造和元数据处理拆为职责明确的内部模块，保留 `persistence` 下的真实入口。
- [x] 3.2 为同步后端补充原子提交失败、索引恢复、压缩记录和分叉写入的边界测试，确认 JSONL 格式及既有用户数据不变。
- [x] 3.3 更新持久化调用方和测试，使其通过新的协议与异步边界访问后端，不在异步方法中直接调用同步文件操作。

## 4. 移除兼容入口并迁移引用

- [x] 4.1 将生产代码、测试和文档中的 `session.bus`、`session.store*`、`session.json_file_store`、`session.tree` 和 `session.session_runtime` 引用迁移到对应职责子包。
- [x] 4.2 删除仅负责转发的根级兼容模块，保留必要的 `codeagent.session` 包级稳定导出，并检查循环导入和导出列表。
- [x] 4.3 增加入口契约测试：真实职责入口可导入并提供预期对象，已移除的兼容模块不可导入。
- [x] 4.4 在架构或迁移文档中记录旧入口到新入口的替代关系和 breaking change，避免后续新增代码重新依赖兼容层。

## 5. 回归验证与质量门禁

- [x] 5.1 增加异步持久化不阻塞事件循环、取消收尾、后台操作确定性和失败回滚的单元/契约测试。
- [x] 5.2 运行 `uv run pytest tests/test_decoupling.py tests/session -q` 及相关集成测试，修复行为回归。
- [x] 5.3 运行 `uv run ruff check src tests scripts`、`git diff --check`、规模扫描和 `openspec validate --specs`，确认边界、规模和规格均满足要求。
- [x] 5.4 运行完整离线测试 `uv run pytest -q`，检查差异中没有临时文件、用户数据、密钥或无关修改，并汇总未覆盖限制。
