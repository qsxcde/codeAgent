## 1. 测量基础

- [x] 1.1 定义 TUI benchmark 的场景、参数、结构化结果数据模型和 JSON 序列化格式。
- [x] 1.2 为事件归约、模型渲染、后端渲染、帧总耗时、Markdown、缓存和峰值内存建立标准库测量工具。
- [x] 1.3 扩展仅供开发/测试使用的渲染统计，使其能导出 block 数、可见范围、缓存命中/未命中和事件计数，而不记录会话正文或密钥。

## 2. 离线场景与基准入口

- [x] 2.1 创建纯 TUI fixture 与 fake backend，覆盖 10/100/1000 历史 block、流式正文、大工具输出、滚动和 resize。
- [x] 2.2 创建会话恢复构建场景，覆盖消息数、正文体积和工具输出体积的不同组合。
- [x] 2.3 提供显式 benchmark 命令，可选择场景、重复次数和 JSON 输出路径，并在结果中记录环境元数据。

## 3. 回归护栏与文档

- [x] 3.1 为结果格式、事件顺序、缓存统计和 fixture 确定性增加快速离线 pytest 断言。
- [x] 3.2 为已知热点增加工作量回归断言，避免普通测试依赖机器特定的绝对耗时。
- [x] 3.3 记录当前基线结果，更新 `docs/iteration/v0.4.md` 和开发命令说明，标注运行方式及结果适用边界。
- [x] 3.4 运行 TUI 专项测试、基准入口和 OpenSpec 校验，确认基线变更不改变用户可见行为。
