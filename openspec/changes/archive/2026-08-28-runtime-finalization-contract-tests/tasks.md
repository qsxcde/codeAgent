## 1. 收尾边界

- [x] 1.1 为成功、失败、取消、持久化失败和压缩失败定义统一 RunOutcome 与提交状态
- [x] 1.2 调整 AgentSession.run() 的执行、提交、usage、压缩、回滚和终态发布顺序
- [x] 1.3 确保 finish_run 只在消息提交/回滚、资源收尾和终态事件完成后执行

## 2. 历史与用量隔离

- [x] 2.1 增加提交失败时消息历史不变、usage 不追加的回归测试
- [x] 2.2 增加自动压缩成功/失败、恢复、继续和分叉后的历史一致性测试
- [x] 2.3 增加成功运行后重复提交、取消后继续和失败后 retry 边界测试

## 3. Runtime 契约矩阵

- [x] 3.1 覆盖多轮 ReAct、并发工具、串行工具、参数错误、工具异常和递归超限
- [x] 3.2 覆盖模型等待、工具等待、确认等待、超时、取消和 cleanup_uncertain
- [x] 3.3 覆盖重复运行、steer/follow-up 隔离、监听器异常和事件终态唯一性
- [x] 3.4 覆盖配置完整透传、run/session/operation 关联和迟到事件抑制

## 4. 验证与文档

- [x] 4.1 先运行受影响的 core/session/contracts 窄测试并修复回归
- [x] 4.2 运行 OpenSpec 校验并将契约测试结果记录到变更说明
- [x] 4.3 保留全量测试由用户/CI 执行，不在本变更中把真实密钥或网络测试纳入默认套件
