## 1. 建立回归测试契约

- [x] 1.1 为 `TuiModel` 增加普通提交准备态、用户预回显与对应 `SESSION_STARTED` 合并的单元测试，验证只保留一条用户消息且真实事件进入运行态
- [x] 1.2 为 TUI 生命周期增加时序测试，记录 backend 首帧、准备状态和 FakeSession/初始化钩子的开始顺序，断言首帧先于模型、工具或工作区初始化
- [x] 1.3 增加提交任务在真实会话开始前失败、取消和任务创建异常的回归测试，验证错误/取消可见、运行态结束且待去重标记不会残留
- [x] 1.4 补充相同文本连续提交、普通文本与斜杠命令、忙碌态草稿及登录输入的回归断言，确认只改变普通文本提交路径

## 2. 实现提交准备态与立即渲染屏障

- [x] 2.1 扩展 TUI 模型的待提交投影，使普通提交同时显示用户消息、准备中活动提示和状态栏阶段，并保持该状态不写入 session 历史或 runtime 事件流
- [x] 2.2 为待提交投影提供与真实开始事件、错误和取消路径配套的去重标记清理，保留已显示的用户消息并避免后续相同文本被错误合并
- [x] 2.3 在渲染协调器中增加受控的立即 flush 能力，复用事件缓冲、模型、backend 和状态栏同步流程，在提交屏障中绕过异步排队和帧节流但不改变普通增量渲染调度

## 3. 调整对话任务启动顺序

- [x] 3.1 修改普通文本提交协调，使其按“写入准备态 → 完成首帧 → 创建并启动任务监督器”的顺序执行，确保屏障完成前不触发模型、工具或工作区初始化
- [x] 3.2 将真实 `SESSION_STARTED` 作为运行态权威，复用待显示用户内容匹配逻辑合并预回显，并在已开始和未开始的终态路径分别完成引用、活动提示和运行标记清理
- [x] 3.3 保持任务完成后的既有事件渲染、验证状态、取消和退出收尾语义，确认首帧屏障不会留下重复排队帧或阻塞后续输入恢复

## 4. 分层验证与 OpenSpec 检查

- [x] 4.1 运行 TUI 模型与生命周期窄测试，并根据失败结果修正时序、去重和终态清理
- [x] 4.2 运行 `uv run pytest -m "unit or contract" -q --strict-markers` 及变更相关的集成测试
- [x] 4.3 运行 `uv run ruff check src tests scripts`、`git diff --check` 和 `openspec validate --specs`，检查差异仅包含本变更所需文件
