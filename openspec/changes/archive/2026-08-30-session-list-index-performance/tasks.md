## 1. 索引命中与失效隔离回归

- [x] 1.1 增加多会话列表/搜索/筛选契约测试，证明所有有效索引命中时不扫描 JSONL 正文。
- [x] 1.2 增加单会话索引缺失、损坏和源指纹过期测试，证明仅受影响目标回源，其它会话仍走索引并返回。
- [x] 1.3 增加索引回源失败隔离测试，证明损坏目标不会阻断其它会话的列表结果。

## 2. 最近会话与文档验收

- [x] 2.1 增加 `continue_recent` 的索引排序和单目标恢复测试，证明候选集合不被逐一加载完整历史。
- [x] 2.2 更新 sessions 主规格、测试指南和 v0.4 进度，记录索引命中、失效隔离和单目标回源边界。
- [x] 2.3 运行相关窄测、unit/contract、全量测试、Ruff、规模扫描、OpenSpec 校验、差异检查和构建检查。
