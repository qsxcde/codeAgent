## 1. 优化前置护栏

- [x] 1.1 确认性能基线变更已提供可重复的场景和初始结果，并确认职责重构后的模块边界可用于独立测试。
- [x] 1.2 增加交错流式事件、缓存淘汰、布局点击映射、恢复过期、忙碌提交和幂等关闭的回归测试。

## 2. 有界布局与缓存

- [x] 2.1 将 Transcript 布局缓存改为按当前 block revision 管理，并实现 clear、block 移除、宽度变化和容量上限的淘汰路径。
- [x] 2.2 建立 block 高度/行范围索引与可见范围二分定位，保证尾部更新和历史工具展开只更新必要后缀。
- [x] 2.3 保持 resize、跟底、上滚、新输出提示和点击工具块的既有语义，并以基准验证长历史工作量下降。

## 3. 流式渲染优化

- [x] 3.1 在 TUI 展示层实现相邻 thinking/text delta 缓冲与帧边界 flush，确保结构事件和终态事件不被跨越或重排。
- [x] 3.2 为活动 assistant block 实现稳定 Markdown 边界缓存、未闭合结构回退和终态完整校正渲染。
- [x] 3.3 验证完成消息、超长纯文本退化、代码围栏、列表和交错工具事件的视觉输出与既有契约一致。

## 4. 恢复与输入体验

- [x] 4.1 以消息数、可见文本体积和工具输出体积估算恢复成本，并在超过任一阈值时后台构建 transcript。
- [x] 4.2 保持 session id 过期保护和取消语义，覆盖大内容少消息会话的恢复。
- [x] 4.3 在运行、恢复或压缩期间保留普通输入草稿并显示可操作反馈，不创建并发对话。

## 5. 运行时生命周期

- [x] 5.1 实现 TUI 的幂等异步 shutdown：停止输入、取消并等待任务、收尾工具和运行时、解除订阅后再退出 backend。
- [x] 5.2 让 AgentRuntime 在关闭后解除注册引用，并让 provider/model/effort 热切换复用同一关闭协议。
- [x] 5.3 覆盖运行中退出、关闭超时、热切换、晚到事件和重复关闭，确保完整退出文档仍可输出。

## 6. 验证与性能对比

- [x] 6.1 使用性能基线的相同场景比较优化前后 render p50/p95、事件到帧比、缓存规模和峰值内存。
- [x] 6.2 运行 TUI、session、MCP 和完整离线测试，以及 OpenSpec 校验；确认事件顺序、会话 append-only、工具确认和最终 Markdown 语义不变。

<!-- TUI、session、MCP、组合根与完整离线测试均已通过，且已完成 OpenSpec 校验。 -->
