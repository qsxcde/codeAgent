## 1. 能力模型与探测

- [x] 1.1 新增不可变的工具能力条目/快照模型，定义稳定 key、available、code、message、detail 字段和序列化/展示接口。
- [x] 1.2 实现离线可注入的能力探测，覆盖真实 Bash、平台标识、`rg`/`fd` PATH 命中、进程清理能力和安全策略装配状态，并对缺失/未知情况返回诊断。
- [x] 1.3 导出公共能力入口，保持现有工具注册表和 core 分层边界不变。

## 2. 组合根与用户诊断

- [x] 2.1 在工具装配路径生成并保留当前能力快照，不启动外部命令、不修改会话状态。
- [x] 2.2 将能力快照接入 TUI `/status`，展示所有能力项的可用性和缺失原因，同时保留既有状态、上下文和用量内容。
- [x] 2.3 增加能力探测、平台模拟、无副作用读取和状态文本的单元/契约回归测试。

## 3. 文档与验收

- [x] 3.1 更新工具主规格、架构/测试文档和 v0.4 迭代记录，说明能力探测与后续 fallback 的边界。
- [x] 3.2 运行相关窄测、unit/contract、全量测试、Ruff、差异检查、构建和 OpenSpec 校验，确认变更可归档。
