## 1. 建立流式交互测试基线

- [x] 1.1 在 `tests/tui/` 增加可控的高频流式 fake session，能够持续产生文本/思考增量并记录输入、取消、滚动和确认事件的顺序与时间。
- [x] 1.2 扩展 TUI 性能夹具，记录帧耗时、控制事件调度延迟、增量合并批次、过期帧和缓存占用；不得记录 prompt 或用户内容。
- [x] 1.3 为控制延迟、帧预算和事件完整性定义稳定的测试辅助断言，时序单测使用注入时钟，避免依赖单次机器抖动。

## 2. 优化事件增量缓冲

- [x] 2.1 重构 `TuiEventBuffer` 的同类型增量累积，使用分段存储和批量合并，避免逐事件复制已累积 payload。
- [x] 2.2 为增量缓冲增加实现定义的批量/大小边界，确保大流式回复不会形成无界单批次，同时保证文本不丢失、不重复。
- [x] 2.3 补充文本与思考增量交替、工具/确认/错误/取消/终态插入、超大单个增量的顺序和完整性回归测试。

## 3. 实现助手回复增量渲染

- [x] 3.1 在 `AssistantBlock` 中建立追加式正文状态，避免每帧重新 join 全部片段，并按终端宽度保存稳定内容、未完成尾部和必要的 Markdown 状态。
- [x] 3.2 让长行、列表、标题、行内结构和代码围栏只解析新增稳定片段；不可安全增量处理时按当前宽度进行受控重建。
- [x] 3.3 保留回合结束的完整 Markdown 渲染和有界缓存，补充流式最终输出与完整渲染结果等价、resize 后结果正确的测试。
- [x] 3.4 验证超过 Markdown 阈值、空内容、未闭合结构和多次终态处理不会抛错或产生重复内容。

## 4. 改造协作式帧调度和首帧路径

- [x] 4.1 在 `TuiRenderCoordinator` 中实现 dirty/generation 管理和可继续的帧准备流程，使布局与 Markdown 工作按有界批次推进并在 loop turn 之间让出事件循环。
- [x] 4.2 只提交仍对应当前 generation 的完整帧；取消、滚动、resize 或新事件到达时淘汰过期帧，不影响事件顺序和最终状态。
- [x] 4.3 保持输入、滚动、确认和取消回调只执行轻量状态操作；取消先触发 session/task abort，再由终态事件更新展示。
- [x] 4.4 为 v4-17 的提交准备态增加轻量首帧路径，确保用户消息和运行状态先于 `session.run` 可见，且不同步重算全部历史或完整长 Markdown。
- [x] 4.5 补充帧调度器在连续增量、控制事件插入、取消、终态和 resize 场景下的协作式调度回归测试。

## 5. 保护工具结果和真实交互链路

- [x] 5.1 确认大工具结果在折叠状态下只渲染摘要，展开状态只处理当前分页/可视内容，并补充缺少结构化 metadata 的兼容测试。
- [x] 5.2 使用 Textual `run_test()` 验证高频流式输出期间仍可输入文本，输入内容不丢失且最终回复完整。
- [x] 5.3 使用真实 Textual 事件循环验证高频输出期间 Esc 取消、滚轮/PageUp/PageDown 滚动和确认键均能在目标延迟内生效。
- [x] 5.4 验证工具调用、确认请求、错误、取消和 `TURN_END` 与其前置增量的视觉顺序不被帧合并破坏。

## 6. 验收与文档同步

- [x] 6.1 运行 TUI 窄测试、相关分层测试和性能场景，确认控制事件 p95 延迟不超过 50ms、最大延迟不超过 100ms，单次渲染工作不超过约 33ms 帧预算。
- [x] 6.2 运行 `uv run ruff check src tests scripts`、`git diff --check`、`openspec validate --specs` 和必要的完整离线测试，修复发现的回归。
- [x] 6.3 更新 `docs/iteration/v0.4.md` 中 V4-18 的完成状态和用户可感知改善，记录未纳入本变更的 V4-21/V4-22 后续范围。
