## ADDED Requirements

### Requirement: 会话分叉

会话 SHALL 支持从既有会话的指定用户消息处分叉出新会话:新会话 SHALL 复制分叉点之前(含该用户消息之前)的完整消息历史;新会话头 SHALL 记录父会话标识;原会话文件 SHALL 不被修改;分叉点 SHALL 限定为 user 消息(分叉语义 = 从该消息之前重新开始);分叉后新会话 SHALL 可继续对话,上下文与分叉点前一致;会话来源 SHALL 可被订阅方感知(分叉产生的会话首轮事件携带父会话标识)。

#### Scenario: 从消息分叉

- **WHEN** 用户从既有会话的某条 user 消息分叉
- **THEN** 创建新会话:历史 = 该消息之前(含)的全部消息,新会话头记录父会话 id,原会话文件与历史保持不变

#### Scenario: 分叉点校验

- **WHEN** 分叉点不是 user 消息(或不存在)
- **THEN** 分叉被拒绝并给出明确错误,不产生任何会话

#### Scenario: 分叉后继续

- **WHEN** 分叉会话继续对话
- **THEN** 新消息追加到新会话文件,上下文与分叉点前一致;原会话不受影响

#### Scenario: 来源可感知

- **WHEN** 会话由分叉产生
- **THEN** 订阅方收到的新会话首轮事件携带父会话标识(对齐 Pi `session_start(reason=fork)` 语义)
