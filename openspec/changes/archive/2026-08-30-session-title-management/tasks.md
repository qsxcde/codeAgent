## 1. 标题契约与存储回归

- [x] 1.1 增加自动/显式标题的空白归一化、长度上限、空标题拒绝和旧格式兼容测试。
- [x] 1.2 增加 JsonFileStore 与 MemoryStore 重命名后读回、重启/重建索引、历史消息和 parentSession 不变测试。

## 2. 会话管理操作

- [x] 2.1 在 SessionManager 增加公开重命名操作，复用 SessionStore 元数据端口并区分无存储、无会话和无效标题错误。
- [x] 2.2 确认重命名不改变运行态、最近活动时间、压缩状态、消息链和 fork 语义。

## 3. TUI 命名入口

- [x] 3.1 注册 `/name` 命令并实现查看当前标题、设置标题和归一化结果反馈。
- [x] 3.2 覆盖空标题、无当前会话、无持久化后端、存储失败和后续 `/sessions`/`/tree` 展示回归。

## 4. 验收与文档

- [x] 4.1 更新 sessions/TUI 主规格、v0.4 状态、测试和架构文档。
- [x] 4.2 运行相关窄测、unit/contract、全量测试、Ruff、规模扫描、OpenSpec 校验、差异检查和构建检查。
