## 1. 外部命令适配

- [x] 1.1 新增不经 shell 的 `rg`/`fd` 可选命令执行边界，支持 timeout、临时文件有界读取和失败信号。
- [x] 1.2 解析 `rg --json` match/context 和 `fd` 路径输出，保持现有路径、glob、上下文、噪声目录与 limit 语义。

## 2. 工具接入与回退

- [x] 2.1 让 `grep` 优先尝试 `rg`，失败或不可用时回退现有纯 Python 实现。
- [x] 2.2 让 `find` 优先尝试 `fd`，失败或不可用时回退现有纯 Python 实现。
- [x] 2.3 补充安全参数、缺失依赖、执行失败、超时、输出限制和结果等价回归。

## 3. 文档与验收

- [x] 3.1 更新主规格、架构、测试和 v0.4 迭代文档，说明可选加速与 fallback 边界。
- [x] 3.2 运行受影响测试、unit/contract、全量测试、Ruff、规模/差异检查、构建和 OpenSpec 校验。
