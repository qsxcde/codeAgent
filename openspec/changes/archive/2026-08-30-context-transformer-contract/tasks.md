## 1. 契约与错误模型

- [x] 1.1 增加 `ContextTransformer` 显式协议并保留 `TransformContext` 兼容导出，明确同步/异步输入输出类型。
- [x] 1.2 增加上下文扩展超时错误及 `AgentLoopConfig.context_transform_timeout` 校验，保持取消不被包装。

## 2. 请求准备实现

- [x] 2.1 在模型请求准备边界物化并校验两类扩展的消息输出，确保非法结果阻断模型且不修改源历史。
- [x] 2.2 按固定顺序和每次扩展调用超时执行 transformer/preparer，保留预算、工具结果治理和 preflight 链路。
- [x] 2.3 更新 core 公开导出与组合根类型接缝，确认现有同步/异步扩展仍可用。

## 3. 回归覆盖与文档

- [x] 3.1 补充契约测试：输入输出隔离、非法返回、顺序、超时、取消、无模型调用和源历史保留。
- [x] 3.2 同步主规格、v0.4 进度和相关公共接口说明，完成 OpenSpec 校验。
- [x] 3.3 运行窄测试、分层测试、全量测试、Ruff、差异检查和构建，记录验证结果。
