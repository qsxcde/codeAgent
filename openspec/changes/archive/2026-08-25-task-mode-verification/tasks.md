## 1. 模式模型与命令解析

- [x] 1.1 在应用层定义 `TaskMode`、粘性模式和单次模式覆盖的数据模型，并为未知模式提供可操作错误
- [x] 1.2 实现 `/ask`、`/plan`、`/code`、`/mode` 的解析，保留普通消息原文和当前会话模式
- [x] 1.3 为 ask/plan/code/auto 建立工具权限配置，确保只读模式在工具边界拒绝写入和变更型 bash
- [x] 1.4 为模式解析、粘性切换、单次覆盖和权限拒绝增加离线单元测试

## 2. 结构化工具执行结果

- [x] 2.1 扩展 `BashInvocationResult`，暴露退出码、执行状态、耗时、输出截断和清理确定性字段
- [x] 2.2 将 bash 结构化字段映射到 `ToolResult` 和会话事件 metadata，兼容现有文本输出和旧消费者
- [x] 2.3 保留 grep 无匹配等退出码豁免的明确结构化语义，并覆盖成功、非零退出、超时、取消和清理不确定测试

## 3. 工作区快照与变更检测

- [x] 3.1 实现任务开始/结束的 Git 工作区基线采集，包含修改、删除、未跟踪文件和差异摘要
- [x] 3.2 为非 Git 目录实现受控快照后备，并支持排除会话存储、临时产物和配置的忽略路径
- [x] 3.3 合并工具写入事件与最终快照结果，保证脚本外部修改仍能触发变更判定
- [x] 3.4 增加无变更、工具写入、bash 脚本写入和恢复原状等回归测试

## 4. 任务监督器与验证命令

- [x] 4.1 在 `src/codeagent/app/` 新增任务监督器，订阅 AgentSession 事件并维护 planning/editing/verifying/repairing 状态
- [x] 4.2 定义 `TaskEvent`、`VerificationResult` 和 `TaskResult`，明确 verified/unverified/failed/cancelled/no_changes 终态
- [x] 4.3 实现验证命令解析，按显式命令、项目配置、常见项目文件探测的优先级选择命令并记录来源
- [x] 4.4 实现有界输出、超时、取消和安全策略下的验证运行器；无命令时返回 unverified
- [x] 4.5 为监督器的状态转移、命令优先级、无命令、成功、失败、超时和取消增加单元/集成测试

## 5. 失败修复闭环

- [x] 5.1 构造包含命令、退出码、变更路径、diff 摘要和错误尾部的有界修复诊断
- [x] 5.2 实现默认一次、硬上限三次的修复-重新检测-重新验证循环，并在每轮发布阶段事件
- [x] 5.3 实现规范化失败指纹和重复失败提前终止，确保不会无限调用模型或测试
- [x] 5.4 覆盖修复成功、达到上限、重复失败、修复期间取消和敏感输出截断测试

## 6. CLI 与 TUI 集成

- [x] 6.1 将 CLI 普通响应路径接入监督器，渲染阶段进度和唯一任务终态，保留 ask/plan 的普通回答体验
- [x] 6.2 在 TUI 增加模式命令处理和当前模式标记，模式拒绝时只展示简洁提示而不展示技能 Markdown
- [x] 6.3 在 TUI 状态栏显示验证命令、尝试次数、耗时和终态摘要，长命令与诊断按现有大输出策略截断/展开
- [x] 6.4 增加独立 `task_active` 锁和任务取消令牌，使验证/修复期间 Esc 可停止模型、工具和验证进程
- [x] 6.5 添加 CLI/TUI 离线事件渲染和取消行为测试，验证输入在终态后恢复可用

## 7. 文档、配置与验收

- [x] 7.1 记录模式命令、自动模式规则、显式验证命令配置、超时和修复次数默认值
- [x] 7.2 更新相关 OpenSpec 说明与开发文档，明确本变更不持久化完整任务验证日志
- [x] 7.3 运行针对性 pytest 和完整 `uv run pytest -q`，修复回归后执行 `openspec validate --specs`
- [x] 7.4 使用最小示例项目验收问答不测试、代码变更触发验证、失败修复和取消四条端到端路径
