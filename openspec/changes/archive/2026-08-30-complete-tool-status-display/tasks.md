## 1. Core 状态与事件契约

- [x] 1.1 在 core 的工具执行契约中定义规范化生命周期状态、清理状态和状态快照字段，并保留 `ok`、`cleanup_uncertain` 等旧值的读取兼容
- [x] 1.2 调整工具批次事件时序：调用进入批次时发布排队事件，获得并发槽位和确认许可后发布开始事件，进度事件只更新过程信息，结束时发布唯一终态
- [x] 1.3 确保工具生命周期事件始终携带 `run_id`、`tool_call_id`、`operation_id`、工具名、状态、耗时及清理/错误诊断，并保持并发结果按原始调用顺序回填模型
- [x] 1.4 补齐超时、取消、拒绝和清理不确定路径的终态收尾，避免重复终态或终态后继续发布普通过程事件

## 2. Session 事件适配与兼容

- [x] 2.1 扩展 session 事件翻译，将 core 生命周期事实映射为现有 `tool_queued`、`tool_started`、`tool_progress`、`tool_finished` 和 `tool_result` 事件，并保留类型化字段与 metadata 双重兼容
- [x] 2.2 将确认请求、批准、拒绝和运行取消与对应 `tool_call_id` 关联，使等待确认和取消无结果的调用都能被上层识别为明确状态
- [x] 2.3 为旧事件和旧 JSONL 结果增加兼容归一化：缺失新字段时安全映射状态，清理或输出完整性未知时不得阻塞恢复

## 3. TUI 工具块状态投影

- [x] 3.1 扩展 TUI 事件投影，按 `tool_call_id` 更新同一工具块，支持事件先到时的占位或暂存，并阻止未知 ID 更新其它工具块
- [x] 3.2 为工具块实现排队、执行中、等待确认、完成、失败、拒绝、超时、取消和清理不确定的明确摘要、图标/样式与耗时展示
- [x] 3.3 让工具块区分执行结论、清理状态和输出完整性，复用现有输出缓冲、截断、分页和导出能力，不通过解析自然语言结果推断状态
- [x] 3.4 实现终态不可回退、重复完成/结果幂等和旧进度忽略规则，确保并发乱序事件不会串改工具块或重复追加输出
- [x] 3.5 在运行取消且工具没有最终结果时，将排队/执行中的工具块收敛为取消或清理不确定并移出活动状态

## 4. Runtime 聚合与布局稳定

- [x] 4.1 按工具状态迁移维护运行时聚合计数，修复 `tool_progress` 增加 running 数量的问题，并区分完成、失败、拒绝、超时、取消和清理不确定
- [x] 4.2 保持底部状态栏的紧凑槽位和窄终端降级行为，使用聚合信息提示当前工具状态，将详细诊断留在可展开工具块
- [x] 4.3 检查流式进度、结果截断、分页和导出路径的渲染性能，确保大结果或高频进度不阻塞输入与确认操作

## 5. 回归测试与文档

- [x] 5.1 增加 core 契约测试，覆盖排队/开始/进度/终态时序、并发调用 ID 归属、唯一终态、超时、取消、拒绝和清理不确定
- [x] 5.2 增加 session 事件翻译和旧 JSONL 恢复测试，覆盖确认状态、取消无结果、旧 `ok`/`cleanup_uncertain` 值和缺失字段
- [x] 5.3 增加 TUI 工具块与语义快照测试，覆盖所有生命周期状态、独立清理/截断展示、乱序事件、重复事件、进度计数和取消收尾
- [x] 5.4 增加跨层回归测试，确认正常工具执行、模型回填顺序、并发上限、状态栏稳定布局和 headless 订阅方行为未改变
- [x] 5.5 更新 v0.4 迭代状态、相关架构/测试文档，并运行 OpenSpec 校验、Ruff、分层测试、完整测试和构建检查
