## 1. 资源配置契约

- [x] 1.1 新增 frozen `ToolResourceLimits`，定义默认值、字段校验、输出/内存有效上限和从配置对象读取的接口。
- [x] 1.2 为 `ProcessRequest` 增加内存预览上限与清理等待时限，补充有限读取/有限等待的进程回归测试。

## 2. 工具与组合根接入

- [x] 2.1 让原子工具和 Bash 使用注入的输出、内存和超时限制，保持未注入时的现有默认行为。
- [x] 2.2 让 `ToolExecutionRuntime` 使用统一并发上限，组合根将同一限制传入工具工厂、runtime 和 session 默认超时。
- [x] 2.3 覆盖取消、超时、排队、非法配置和清理不确定的结构化状态回归。

## 3. 文档与验收

- [x] 3.1 更新主规格、架构、测试和 v0.4 迭代文档，记录资源配置和内存边界。
- [x] 3.2 运行受影响测试、unit/contract、全量测试、Ruff、规模/差异检查、构建和 OpenSpec 校验。
