## 1. 建立恢复回归测试基础设施

- [x] 1.1 在 `tests/session/behavior/` 增加可参数化的 `MemoryStore` / `JsonFileStore` 夹具，并提供创建会话、重新构造实例和读取完整物理历史的辅助函数
- [x] 1.2 增加逻辑上下文快照辅助函数，规范化比较摘要、压缩 entry、消息 ID/角色/内容、父级 ID、工具调用 ID、工具结果 metadata 与累计 usage
- [x] 1.3 增加确定性 FakeClient、summarizer 和预算配置，确保模型实际请求、摘要合并和模型窗口切换均可离线断言

## 2. 实现会话恢复与压缩回归

- [x] 2.1 覆盖未压缩会话的“成功提交 → 新实例恢复 → 继续对话”流程，验证消息内容、父级链、同一会话追加和恢复后的模型请求上下文
- [x] 2.2 覆盖单次压缩后的冷启动恢复，分别断言逻辑上下文与物理 JSONL 历史，验证摘要虚拟消息只进入模型请求且不落盘
- [x] 2.3 覆盖多次压缩后的恢复，验证最新摘要、最新 `firstKeptEntryId`、压缩 entry 父级链、摘要合并和已压缩消息不重复进入上下文
- [x] 2.4 对两种 Store 运行相同恢复与压缩场景，修复并固化后端之间可观察行为不一致的问题
- [x] 2.5 增加结构化工具结果 metadata、旧 JSONL 缺失 metadata 和无 artifact 截断结果的恢复回归，验证 unknown/legacy 与 incomplete/unavailable 状态不被误报为完整

## 3. 实现分叉与模型切换恢复回归

- [x] 3.1 覆盖压缩会话从保留窗口内 user 消息分叉，验证子会话摘要、保留消息、`parentSession`、父级链和继续对话行为
- [x] 3.2 覆盖压缩边界之前分叉，验证子会话只保留摘要语义、不复制已摘要物理消息，且父会话文件与压缩记录不变
- [x] 3.3 覆盖恢复压缩会话后切换到更小和更大窗口模型，验证下一次预算使用新模型窗口/输出预留，历史、摘要、压缩链和 usage 不变
- [x] 3.4 覆盖恢复后自动压缩预算重新计算，验证预算只统计逻辑上下文，不因物理保留消息重复触发或增加估算
- [x] 3.5 若上述回归暴露生产缺陷，按失败边界对持久化读取、父级链接、分叉状态复制或预算重建实施最小修复，并保留对应回归用例

## 4. 实现 TUI 恢复回归

- [x] 4.1 覆盖 TUI 对压缩摘要、保留窗口消息和结构化工具结果 metadata 的恢复投影，验证物理已摘要消息不重复展示、不可恢复结果状态可见
- [x] 4.2 使用可控异步恢复任务覆盖大会话恢复和会话切换竞态，验证迟到的旧恢复结果不覆盖当前 transcript、状态栏或输入状态
- [x] 4.3 覆盖 TUI 恢复失败与旧会话 tool metadata 缺失场景，验证当前会话保持可用并显示明确诊断

## 5. 分层验证与文档收尾

- [x] 5.1 运行新增会话、压缩、分叉、预算、工具结果和 TUI 窄测，修复测试隔离、异步收尾或平台相关问题
- [x] 5.2 运行 `uv run pytest -m "unit or contract" -q --strict-markers`、`uv run pytest -q`、`uv run ruff check src tests scripts` 和 `git diff --check`
- [x] 5.3 运行 `openspec validate --specs`，确认本 change 的 proposal、delta specs、design 和 tasks 均满足 schema
- [x] 5.4 更新 `docs/iteration/v0.4.md` 的 V4-15 状态与验证证据，并检查变更差异不包含无关文件、敏感信息或生成产物
