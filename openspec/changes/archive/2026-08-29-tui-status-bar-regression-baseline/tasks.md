## 1. 任务状态计时

- [x] 1.1 梳理现有 runtime 与 task 状态投影，明确活动阶段、终态和状态栏计时的单一显示语义
- [x] 1.2 为 TUI 任务显示状态增加注入时钟驱动的阶段开始时间、当前耗时和终态冻结值
- [x] 1.3 在任务阶段切换、重试、取消、验证失败、验证完成和无变更路径中正确重置或冻结任务耗时
- [x] 1.4 保持 Agent runtime 耗时与任务验证/修复耗时相互独立，并保留现有 `/status` 诊断语义

## 2. 状态栏刷新调度

- [x] 2.1 在 `TuiRenderCoordinator` 中增加低频状态计时刷新任务，仅在 runtime 或 task 处于活动阶段时启动
- [x] 2.2 让状态刷新复用现有帧调度、generation 和 backend 提交流程，不执行模型、工具或会话轮询
- [x] 2.3 在终态、Esc 取消、退出和 shutdown 路径中停止并回收状态计时任务
- [x] 2.4 验证状态计时刷新不会阻塞输入、滚动、确认和取消，也不会与活动提示动画重复调度

## 3. 状态栏布局回归基线

- [x] 3.1 增加状态栏三栏区域在 runtime 阶段、任务状态、临时提示和上下文数据变化前后的 cell 边界断言
- [x] 3.2 覆盖 `09.9s` 到 `10.0s`、上下文 token/百分比位数变化和未知上下文占用的稳定性测试
- [x] 3.3 覆盖所有任务终态 `verified`、`unverified`、`failed`、`cancelled`、`no_changes` 及重试过程的显示与槽位稳定性
- [x] 3.4 覆盖现有宽度断点及相邻宽度、长验证命令、长路径、中文/全角混合文本和无溢出优先级
- [x] 3.5 覆盖任务阶段耗时独立于 Agent runtime、无新事件时持续刷新和终态冻结的注入时钟单测

## 4. TUI 协调器与交互回归

- [x] 4.1 使用可控异步时钟或短等待 fake backend 验证无新 Agent 事件时状态栏仍产生受控更新
- [x] 4.2 验证状态计时期间输入、滚动、确认和 Esc 回调仍按原顺序执行且不被延迟到下一长帧
- [x] 4.3 验证任务终态和 shutdown 后不再提交状态计时帧或遗留后台任务
- [x] 4.4 保留并更新 `TuiBackend.set_status()`、`/status` 和既有状态栏语义测试

## 5. 文档与验证

- [x] 5.1 同步并校验 `openspec/specs/tui/spec.md` 的状态栏和任务验证状态契约
- [x] 5.2 更新 `docs/iteration/v0.4.md`，将 V4-19 标记为回归基线已完成并记录验证范围
- [x] 5.3 运行 TUI 窄测试、unit/contract 测试和必要的异步集成测试
- [x] 5.4 运行 `uv run ruff check src tests scripts`、`git diff --check`、`openspec validate --specs`、完整离线测试和构建检查
