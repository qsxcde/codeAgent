## 1. 建立迁移清单

- [x] 1.1 按行为域登记现有测试文件、测试数量和目标文件
- [x] 1.2 确认 test-foundation-stability 已完成并采用其 marker 与 fixture 约定
- [x] 1.3 标记长期公共契约测试和迁移期兼容测试

## 2. 拆分大型测试文件

- [x] 2.1 将 TUI view 测试拆分为生命周期、命令、会话、确认、状态和扩展能力测试
- [x] 2.2 将 session store 测试拆分为 JSONL、MemoryStore、索引、分叉、压缩和用量测试
- [x] 2.3 将 session 行为测试拆分为运行、恢复、取消、确认、压缩和用量测试
- [x] 2.4 将组合根测试拆分为装配、运行时生命周期、Provider、Session 和 TUI 装配测试
- [x] 2.5 将工具测试拆分为原子工具、bash、执行器、安全和共享文件系统测试
- [x] 2.6 每完成一个文件迁移，核对测试收集数量和测试名称，避免遗漏或重复

## 3. 建立契约测试

- [x] 3.1 为 JSONL Store 和 MemoryStore 抽取共享的会话 CRUD、消息、分叉和恢复场景
- [x] 3.2 为 Provider/ChatClient 建立生成、流式事件、usage、错误和关闭行为契约
- [x] 3.3 为 AtomicTool 和 AgentTool 建立输入校验、结果、错误和取消行为契约
- [x] 3.4 将 core/session、ai/tools/session 等分层边界测试集中到 contracts 目录

## 4. 补齐高风险行为

- [x] 4.1 为 `last_activity_at` 增加模型、持久化、内存存储、索引和最近会话排序测试
- [x] 4.2 增加会话恢复、失败回滚、重试、取消和后台任务清理的集成测试
- [x] 4.3 增加 MCP server 启动、工具发现、调用、超时和关闭测试
- [x] 4.4 增加 bash 进程树清理、路径边界和跨平台命令差异测试
- [x] 4.5 增加 CLI 真实入口和 TUI 关键用户流程测试
- [x] 4.6 完成 `last_activity_at` 对应产品实现并确认新增测试与模型、两种存储后端、索引和最近会话排序保持一致

## 5. 清理测试契约

- [x] 5.1 删除或迁移已经废弃的兼容入口测试
- [x] 5.2 为仍需保留的兼容入口补充生命周期说明和专用 marker
- [x] 5.3 为 TUI 关键渲染场景保留少量稳定语义快照，避免全面依赖终端快照
- [x] 5.4 更新 tests 目录说明和测试运行文档
