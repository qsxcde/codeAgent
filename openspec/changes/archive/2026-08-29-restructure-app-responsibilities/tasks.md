## 1. 建立重构护栏

- [x] 1.1 为 CLI/TUI 的输入、命令、会话切换与恢复、取消、退出和关键渲染结果补齐离线特征测试。
- [x] 1.2 记录 `app/` 目标职责映射，并扩展 AST 导入边界测试，限制 Textual 具体类型和 `ai`/`tools`/`session` 基础设施依赖的出现位置。
- [x] 1.3 提取应用层安全错误报告机制，迁移现有宽泛捕获点，使用户提示保持稳定且日志保留操作、对象和 traceback 上下文。
- [x] 1.4 添加可维护的规模检查，覆盖本次重构涉及的应用生产模块，并为保留的兼容外观记录最小且明确的例外理由。

## 2. 拆分 TUI 展示、交互与会话协调

- [x] 2.1 将 `textual_backend.py` 拆为 Textual 布局/小部件、展示适配和 backend 生命周期职责，确保具体 Textual 依赖不向协调层泄漏。
- [x] 2.2 将 `command_coordinator.py` 与 `interaction.py` 拆为输入解析/补全、命令分派以及配置或外部操作适配，并保持命令结果与确认语义不变。
- [x] 2.3 将 `session_coordinator.py` 与 `conversation_coordinator.py` 拆为会话切换/恢复、树操作和对话生命周期职责，保持过期恢复结果保护与取消语义。
- [x] 2.4 将 `runtime.py`、`model.py`、`view.py`、`transcript.py` 和 `blocks.py` 收敛为运行时调度、事件投影、视图外观、记录渲染和块状态等内聚模块。
- [x] 2.5 迁移 TUI 内部导入和工厂装配，删除无用兼容入口，并为每个迁移批次运行 TUI 专项与导入边界测试。

## 3. 拆分组合、技能与任务职责

- [x] 3.1 将 `composition/model_factory.py` 拆为模型配置解析、消息/工具适配和模型装配；将 `tui_factory.py` 收敛为 TUI 依赖组装。
- [x] 3.2 拆分 `skills.py` 与 `skill_packages.py` 的发现、元数据解析、安装/更新和用户提示职责，保留现有包解析与安全检查行为。
- [x] 3.3 拆分 `task_supervisor.py` 与 `task_verification.py` 的任务生命周期、验证策略和结果呈现职责，保持任务终止、异常和验证结果语义。
- [x] 3.4 拆分仍超规模的入口或协调模块（包括 `main.py`），使每个生产文件满足规模规范且依赖仅沿既定方向流动。

## 4. 收敛、文档与验证

- [x] 4.1 扫描并迁移所有生产与测试中的旧导入；确认没有遗留循环导入、重复实现或模块级可变服务状态。
- [x] 4.2 更新 `docs/` 中的应用层职责说明和必要的开发指引，说明组合根、TUI 边缘与错误报告约定。
- [x] 4.3 运行应用/TUI 窄测试、`tests/test_decoupling.py`、Ruff、规模扫描、`git diff --check` 和完整离线测试；修复发现的回归。
