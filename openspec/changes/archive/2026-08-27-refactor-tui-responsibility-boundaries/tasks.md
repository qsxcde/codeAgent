## 1. 行为与导入护栏

- [x] 1.1 固化当前 TUI 的 fake backend 回归场景，覆盖输入、命令、确认、会话切换、滚动、恢复、取消和退出文档。
- [x] 1.2 扩展 AST 导入边界测试，明确富文本基础层、TUI 逻辑层和 Textual 引擎适配层的允许依赖方向。

## 2. 渲染基础与组件层拆分

- [x] 2.1 提取富文本基础类型、宽度、截断和转换工具，并迁移 Markdown renderer 与 backend 的依赖以消除循环导入。
- [x] 2.2 提取用户、助手、工具、错误、取消和活动消息块，保持样式、折叠和输出分页行为不变。
- [x] 2.3 提取 Transcript 的 block 顺序、缓存、滚动、可见范围和点击映射，并迁移对应测试。
- [x] 2.4 提取 StatusBar/FooterInfo 与 TuiModel 的事件投影职责，保持 RuntimeReducer 的输入输出和渲染结果不变。

## 3. 顶层协调职责拆分

- [x] 3.1 提取渲染协调器，承接帧调度、resize、防抖、活动动画和 backend flush。
- [x] 3.2 提取交互与命令协调器，承接输入解析、补全、picker、登录和命令分派。
- [x] 3.3 提取会话恢复协调器，承接会话切换、tree/fork/compact/retry/continue 和过期恢复结果保护。
- [x] 3.4 提取对话任务与确认协调器，承接 TaskSupervisor、确认响应、打断和任务状态展示。

## 4. 装配收敛与验证

- [x] 4.1 将 `TuiApp` 收敛为回调注册、控制器装配和顶层生命周期外观，并更新 `tui_factory.py` 注入方式。
- [x] 4.2 迁移生产代码和测试的旧模块导入，删除不再需要的兼容入口或保留最小内部 re-export。
- [x] 4.3 运行 TUI 专项测试、分层导入测试和完整离线测试，核对命令、渲染、会话、确认和退出行为没有变化。
