## 1. 状态栏布局模型

- [x] 1.1 在状态栏内部定义会话信息区、运行状态区和上下文信息区的职责与宽度预算
- [x] 1.2 使用终端 cell 宽度计算区域边界，并实现区域级补齐、截断和右对齐辅助逻辑
- [x] 1.3 为上下文区保留稳定的右侧预算，在数据未知时显示固定宽度的空态或占位内容

## 2. 动态字段排版

- [x] 2.1 将模型、思考强度和工作目录迁移到会话信息区，并保持会话区边界稳定
- [x] 2.2 将任务状态、模式、运行阶段、当前操作和阶段耗时迁移到运行状态区
- [x] 2.3 统一耗时、任务尝试次数、token 数和百分比的固定格式，避免位数变化导致跳动
- [x] 2.4 将上下文同步、新输出等临时提示纳入运行区的受控位置，不再动态追加尾部字段
- [x] 2.5 实现长操作、长命令和长路径在所属区域内的 cell 宽度截断
- [x] 2.6 实现窄终端降级优先级，确保运行阶段、当前操作和上下文占用优先保留且不溢出

## 3. 渲染验证

- [x] 3.1 增加阶段切换前后会话区和上下文区 cell 起始位置不变的测试
- [x] 3.2 增加 `09.9s` 到 `10.0s`、token 位数变化和百分比变化不移动区域边界的测试
- [x] 3.3 增加任务状态出现、重试、终态收敛和临时提示变化的布局稳定性测试
- [x] 3.4 增加长命令、长路径、中文混合文本和窄终端的截断/无溢出测试
- [x] 3.5 保留并更新现有状态栏语义、样式标签和 `TuiBackend.set_status()` 契约测试

## 4. 验证与交接

- [x] 4.1 运行 focused TUI 测试并确认新增布局断言通过
- [x] 4.2 运行源码编译检查和 `openspec validate --specs`
- [x] 4.3 检查变更范围，确认未修改 core、session、ai、tools 和运行事件协议
- [ ] 4.4 将全量测试留给用户执行，并根据结果决定是否归档变更
