## 1. 会话树数据视图(纯函数)

- [x] 1.1 新建 `build_tree(refs)` 纯函数(独立模块,零 I/O):父会话 → 子分支;孤儿独立根;同级时间序;循环防御性诊断
- [x] 1.2 补纯函数测试:fork 链组织、孤儿独立根、深层链/多根、同级排序、环诊断

## 2. TUI /tree 命令

- [x] 2.1 `commands.py` 注册 `tree` 命令(可选 `session-id` 参数)
- [x] 2.2 `_cmd_tree`:无参 → 展示当前会话所在 fork 链树(缩进 + 分支字符,含标题与 id);`/tree <id>` → `manager.switch` 切换(订阅跟随)并反馈;会话不存在就地报错;无会话空态
- [x] 2.3 补测试:无参展示、切换节点、会话不存在、空态

## 3. /sessions list 父子缩进展示

- [x] 3.1 `_cmd_sessions` 的 `list` 分支改为树形缩进渲染(复用 `build_tree`,父行 + 子分支缩进;孤儿平级)
- [x] 3.2 更新既有平铺断言为树形断言;补孤儿平级测试

## 4. 收尾

- [x] 4.1 全量 `uv run pytest` 全绿(零网络零密钥);`openspec validate` 通过
