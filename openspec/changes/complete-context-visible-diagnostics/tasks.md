## 1. 统一诊断契约

- [x] 1.1 在 `core/context` 增加与 provider、session 和终端无关的上下文诊断数据模型，覆盖窗口来源/确定性、预算组成、preflight 状态、压缩状态与工具结果治理状态，并为未知值定义显式表示。
- [x] 1.2 为诊断模型增加只读快照和稳定格式化所需的辅助接口，确保读取不会触发预算重算、模型请求、压缩或持久化写入。
- [x] 1.3 为完整预算、窗口 fallback/uncertain、无运行记录和失败请求补充 core 单元测试，验证估算值、实际 usage 和累计 usage 不被混淆。

## 2. Session 运行态与事件适配

- [x] 2.1 将现有预算快照、preflight 结果、压缩事件和工具治理元数据合并到 session 运行态诊断快照，并保留最近一次压缩的稳定范围、原因和前后估算。
- [x] 2.2 接入预算计算、preflight、压缩开始/结束、工具结果治理、模型切换和会话恢复路径；成功、跳过、失败、取消及收尾不确定必须保持可区分。
- [x] 2.3 保证诊断数据只保存在运行态，不扩展或改写 JSONL；增加恢复、分叉、失败和重复读取的契约测试，验证消息、父级关系、usage 和压缩记录保持不变。

## 3. CLI 诊断入口

- [x] 3.1 在现有 headless CLI 增加 `--context` 显式入口，支持当前新会话、`--continue` 和 `--session` 场景，并保证查看模式不启动模型或工具。
- [x] 3.2 实现上下文诊断 CLI 渲染，按预算摘要、组成明细、preflight、最近压缩和工具结果治理分组输出；未知、不确定、未发生和未提供状态使用稳定文本。
- [x] 3.3 保持默认 headless 回复、用量尾行和现有脚本输出兼容；增加 CLI 参数、输出内容和无副作用测试。

## 4. TUI 完整诊断面板

- [x] 4.1 在 TUI 命令注册与分发链路中接入 `/context`，为无会话、无诊断和诊断能力缺失提供明确空态或提示。
- [x] 4.2 实现可滚动的上下文诊断展示，按统一快照展示当前预算、组成、窗口来源、preflight、压缩详情和工具结果治理详情。
- [x] 4.3 保持 V4-19 状态栏固定槽位和紧凑摘要行为不变，补充 TUI 命令、面板布局、未知状态及查看无副作用的回归测试。

## 5. 跨层验证与文档

- [x] 5.1 增加 core/session 与 CLI/TUI 之间的契约测试，覆盖正常、接近上限、超预算、不确定窗口、压缩成功/跳过/失败/取消、工具结果治理、恢复和模型切换场景。
- [x] 5.2 运行相关窄测试、unit/contract 测试和完整离线测试，修复诊断快照合并、输出格式或 TUI 回归问题。
- [x] 5.3 运行 Ruff、`git diff --check`、OpenSpec 校验和构建检查，确认没有新增依赖、敏感数据或持久化格式变更。
- [x] 5.4 更新 `docs/iteration/v0.4.md`，将 V4-16 状态、用户入口和验证结果同步为实际完成情况。
