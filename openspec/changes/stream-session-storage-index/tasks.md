## 1. 流式读取基础

- [x] 1.1 将 `JsonFileStore` 的内部 JSONL 迭代器改为文件句柄逐行读取，并保留空行跳过、非法 JSON 行容错以及 header/version 校验行为
- [x] 1.2 重构元数据扫描、用量聚合和 `load_messages` 调用路径，移除原始文本、分行数组和完整 entry 列表的重复保留
- [x] 1.3 重构 `load_context`，按最近压缩切点流式恢复摘要和切点后的消息，保持现有压缩恢复语义

## 2. 可重建会话索引

- [x] 2.1 定义私有索引记录、格式版本、源文件大小/`st_mtime_ns` 指纹及同目录索引路径，并实现原子写入和会话目录权限控制
- [x] 2.2 实现从 JSONL 流式重建索引，聚合标题、模型配置、父会话、用量、最近压缩切点及所需元数据
- [x] 2.3 实现索引读取校验：处理缺失、损坏、格式不兼容和源指纹过期，并在失败时回退到直接流式扫描
- [x] 2.4 在创建会话和追加消息、元数据、模型变更、用量、压缩记录后增量更新索引；索引更新失败时保持 JSONL 写入成功并使索引失效
- [x] 2.5 让 `list`、`get` 和 `load_usage` 优先使用有效索引，同时保留直接流式扫描结果作为权威回退

## 3. 流式分叉

- [x] 3.1 将文件后端 `fork` 改为两遍流式扫描：第一遍定位目标 user 消息和最近压缩状态，第二遍逐行写入目标会话
- [x] 3.2 保持分叉的目标消息边界、压缩摘要、父会话关系、header/version 和源文件不变语义，并处理目标写入失败时的临时文件清理

## 4. 回归与性能验证

- [x] 4.1 增加无索引旧会话、空行、损坏 JSON 行、缺失 header 和不兼容版本的读取回归测试
- [x] 4.2 增加索引创建、追加更新、有效命中、损坏/过期重建、更新失败回退和权限行为测试
- [x] 4.3 增加列表、查询、用量、模型/元数据变更、压缩后的上下文恢复结果与现有行为一致性测试
- [x] 4.4 增加大历史上下文恢复和分叉测试，验证不构造完整 entry/原始文本副本并保持源会话不变
- [x] 4.5 运行 session store、session manager、core、CLI/TUI 相关现有测试，确认公共协议和 `MemoryStore` 未受影响
